# Task 1.3: Build Primitive SearchInput Component

## Recommended Model Tier
🟠 **Gemini 3.6 Flash (High)**

## Objective
Implement canonical `SearchInput.js` primitive supporting controlled query state, focus highlight, clear button, and size variants (`sm`, `md`, `lg`).

## Target Files
- [NEW] [SearchInput.js](file:///d:/Magazine/_PigmentShop/src/components/Search/SearchInput.js)

## Instructions
1. Implement single `SearchInput` component handling leading SearchIcon, text input, trailing clear IconButton, focus/blur state highlights, and theme resolution via `useSearchTheme`.
2. Support `value`, `onChangeText`, `placeholder`, `onClear`, `size`, and `isDark` props.
