# TODO: Tokenize inline styles — EmptyState (DataTable)

**File:** `src/components/DataTable/EmptyState.js`

## Issues
- [ ] L10 — `style={style}` passed to `GlobalEmptyState` — verify forwarded style only contains token references
