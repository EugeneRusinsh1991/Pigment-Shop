# TODO: Tokenize inline styles — Breadcrumb

**File:** `src/components/Navigation/Breadcrumbs/Breadcrumb.js`

## Issues
- [x] L20 — `style={textStyle}` on `Text` (non-link) — verify `textStyle` built from typography/color tokens
- [x] L29 — `style={textStyle}` on `Text` (link) — same as above
