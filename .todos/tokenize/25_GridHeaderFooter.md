# TODO: Tokenize inline styles — GridHeaderFooter

**File:** `src/features/catalog/GridHeaderFooter.js`

## Issues
- [ ] L44 — `style={itemStyle}` — verify item style spacing/sizing from tokens
- [ ] L50 — `style={textStyle}` on `Text` — verify typography tokens
- [ ] L59 — `style={getSortDropdownStyles(isDark)}` — verify helper returns only token-based values
- [ ] L84 — `style={buttonStyle}` — verify button spacing/color from tokens
- [ ] L87 — `style={textStyle}` on `Text` — verify typography tokens
- [ ] L93 — `style={buttonStyle}` — same as L84 (second usage)
- [ ] L96 — `style={textStyle}` on `Text` — same as L87 (third usage)
