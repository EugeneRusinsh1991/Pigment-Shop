# TODO: Tokenize inline styles — SharedFormComponents (features/admin)

**File:** `src/features/admin/SharedFormComponents.js`

## Issues
- [x] L23 — `style={resolvedStyles.row}` — verified `row` uses layout.spacing tokens
- [x] L25 — `style={resolvedStyles.text}` — verified `text` references tokenized styles

> Note: Duplicate of `src/components/Admin/SharedFormComponents.js` — consider consolidating.
