# TODO: Tokenize inline styles — LanguageSelector

**File:** `src/features/shell/NavMenu/LanguageSelector.js`

## Issues
- [ ] L50 — `style={getThemeToggleStyle(isDark)}` — verify helper returns only token-based color/spacing values
- [ ] L53 — `style={themeIconStyles.container}` — verify icon container sizing/padding from tokens
