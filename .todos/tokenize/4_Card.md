# TODO: Tokenize inline styles — Card

**File:** `src/components/Card/Card.js`

## Issues
- [ ] L42 — `style={combinedStyle}` on `Animated.View` — verify spacing/color from tokens
- [ ] L47 — `style={staticStyles.touchable}` — verify touchable hit-area/padding from tokens
- [ ] L61 — `style={combinedStyle}` on `View` ref — same as L42
- [ ] L70 — `style={slotStyles.imageContainer}` — verify image container sizing/spacing tokens
