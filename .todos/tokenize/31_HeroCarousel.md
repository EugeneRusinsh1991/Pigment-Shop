# TODO: Tokenize inline styles — HeroCarousel

**File:** `src/features/home/components/HeroCarousel.js`

## Issues
- [ ] L64 — `style={placeholderStyle}` on `View` — verify placeholder dimensions from size/spacing tokens
- [ ] L71 — `style={baseStyle}` on `View` — verify base container from tokens
- [ ] L72 — `style={localStyles.carouselContainer}` — verify carousel container spacing/sizing from tokens
