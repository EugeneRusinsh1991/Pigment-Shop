# TODO: Tokenize inline styles — PlaceholderCard

**File:** `src/features/catalog/PlaceholderCard.js`

## Issues
- [ ] L51 — `style={getItemStyle(itemWidth)}` — verify `itemWidth` is derived from a layout/size token, not a raw pixel value
