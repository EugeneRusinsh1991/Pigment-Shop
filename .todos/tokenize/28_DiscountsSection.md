# TODO: Tokenize inline styles — DiscountsSection

**File:** `src/features/home/components/DiscountsSection.js`

## Issues
- [ ] L23 — `style={headerStyle}` on `ScrollFadeUp` — verify header spacing from tokens
- [ ] L68 — `style={localStyles.emptyContainer}` — verify empty container spacing from tokens
- [ ] L69 — `style={localStyles.emptyText}` on `Text` — verify typography/color tokens used
