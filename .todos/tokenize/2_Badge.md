# TODO: Tokenize inline styles — Badge

**File:** `src/components/Badge/Badge.js`

## Issues
- [ ] L77 — `style={mergedContainerStyle}` on `Animated.View` — verify container style uses token-based spacing/color
- [ ] L93 — `style={mergedContainerStyle}` on `Animated.View` ref — same as above
