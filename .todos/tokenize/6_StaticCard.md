# TODO: Tokenize inline styles — StaticCard

**File:** `src/components/Card/StaticCard.js`

## Issues
- [ ] L61 — `style={computedStyle}` on `ScrollFadeUp` ref — verify computed style uses token-based spacing/color
