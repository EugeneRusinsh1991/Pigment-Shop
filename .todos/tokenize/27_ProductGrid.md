# TODO: Tokenize inline styles — ProductGrid

**File:** `src/features/catalog/ProductGrid.js`

## Issues
- [ ] L32 — `style={getItemStyle(itemWidth)}` — verify `itemWidth` derived from layout/size token
- [ ] L53 — `style={getGridStyle(isNarrow, gridWidth)}` — verify `gridWidth` and grid gap from tokens
