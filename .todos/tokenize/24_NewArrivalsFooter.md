# TODO: Tokenize inline styles — NewArrivalsFooter

**File:** `src/features/catalog/components/NewArrivalsFooter.js`

## Issues
- [ ] L41 — `style={localStyles.emptyContainer}` — verify empty container spacing from tokens
- [ ] L42 — `style={localStyles.emptyText}` on `Text` — verify text style uses typography/color tokens
