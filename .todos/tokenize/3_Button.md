# TODO: Tokenize inline styles — Button

**File:** `src/components/Button/Button.js`

## Issues
- [ ] L50 — `style={textCombinedStyle}` on `Text` — ensure combined style uses token-based typography
- [ ] L60 — `style={containerStyle}` — ensure container spacing/color from design tokens
