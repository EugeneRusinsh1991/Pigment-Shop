# TODO: Tokenize inline styles — AutocompleteSearch

**File:** `src/components/Search/AutocompleteSearch.js`

## Issues
- [ ] L69 — `style={SearchStyles.autocompleteContainer}` — verify all properties inside `autocompleteContainer` use design tokens (spacing, color, border)
