# TODO: Tokenize inline styles — SearchDropdown

**File:** `src/components/Search/SearchDropdown.js`

## Issues
- [x] L29 — `style={SearchStyles.resultImage}` — verified image sizing uses spacing/size tokens
- [x] L46 — `style={SearchStyles.dropdownOverlay}` — applied theme container tokens (background/border) and z-index from tokens
- [x] L75 — `style={SearchStyles.dropdownOverlay}` — applied theme container tokens (background/border) and z-index from tokens
- [x] L76 — `style={SearchStyles.scrollView}` — verified scroll view sizing/spacing from tokens
