# TODO: Tokenize inline styles — SearchDropdown

**File:** `src/components/Search/SearchDropdown.js`

## Issues
- [ ] L29 — `style={SearchStyles.resultImage}` — verify image sizing uses spacing/size tokens
- [ ] L46 — `style={SearchStyles.dropdownOverlay}` — verify overlay color/z-index from tokens
- [ ] L75 — `style={SearchStyles.dropdownOverlay}` — same as L46
- [ ] L76 — `style={SearchStyles.scrollView}` — verify scroll view sizing/spacing from tokens
