# Phase 3: Migration & Alignment (Mass Clean-up)

## Goal
Remove custom inline and stylesheet typography overrides across all features, migrating them fully to standardized `Text` and `Heading` variants established in Task 2.

## Execution Subtasks

### 3.1 Shell & Navigation Features
- Clean font overrides in `src/features/shell/components/Footer.js`.
- Clean font overrides in `src/features/shell/AppHeader/AppHeaderLogo.js`.

### 3.2 Main User Features
- Clean font overrides in `src/features/orders/OrdersPageStyles.js`.
- Clean font overrides in `src/features/favorites/FavoritesPageStyles.js`.
- Clean font overrides in `src/features/profile/ProfilePageStyles.js`.
- Clean font overrides in `src/features/product/ProductReviewsStyles.js`.

### 3.3 Contact & Miscellaneous Features
- Clean font overrides in `src/features/contact/SocialButtons.js`.

## Dependency
Must be executed AFTER completing [Task 2](file:///d:/Magazine/_PigmentShop/.todos/typography-alignment/task_2_primitive_enforcement.md).
