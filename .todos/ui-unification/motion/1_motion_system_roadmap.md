# Phase 3: Motion Primitives Refactoring Roadmap

## Goal
Extract and unify motion and animation UI wrappers (`ScrollFadeUp`, `PageTransition`) into `src/components/Motion/`.

## Core Tasks
1. Extract `ScrollFadeUp` into `src/components/Motion/ScrollFadeUp/`.
2. Extract `PageTransition` into `src/components/Motion/PageTransition/`.
