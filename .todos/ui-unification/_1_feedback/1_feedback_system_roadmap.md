# Phase 1: Feedback System Refactoring Roadmap

## Goal
Extract and unify feedback UI components (`Toast`, `Skeleton`, `EmptyState`, `InlineError`) into `src/components/Feedback/`.

## Core Tasks
1. Extract `Toast` into `src/components/Feedback/Toast/`.
2. Extract `SkeletonLoader` into `src/components/Feedback/Skeleton/`.
3. Extract `EmptyState` into `src/components/Feedback/EmptyState/`.
4. Extract `FieldError` into `src/components/Feedback/InlineError/`.
