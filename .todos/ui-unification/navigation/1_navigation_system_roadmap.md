# Phase 2: Navigation Primitives Refactoring Roadmap

## Goal
Extract and unify navigation UI components (`Breadcrumb`, `Pagination`) into `src/components/Navigation/`.

## Core Tasks
1. Extract `Breadcrumb` into `src/components/Navigation/Breadcrumbs/`.
2. Extract `PageNavigation` into `src/components/Navigation/Pagination/`.
