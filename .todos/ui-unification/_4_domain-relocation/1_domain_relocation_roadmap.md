# Phase 4: Business Component Domain Extraction Roadmap

## Goal
Relocate domain-specific sections from root `src/components/` into proper feature folders in `src/features/`.

## Core Tasks
1. Move `HeroCarousel`, `FeaturedSections`, `DiscountsSection` to `src/features/home/`.
2. Move `Footer`, `NewArrivalsFooter` to `src/features/shell/` / `src/features/catalog/`.
3. Move `OrderCard` to `src/features/orders/`.
