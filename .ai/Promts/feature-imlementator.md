Read the document referenced above. Review the task description and then execute every implementation prompt listed at the end of the document one by one, in order.

After completing each prompt:
1. Mark it as completed (✔) in the document.
2. Create a backup using:
   backup -- --step "<stepName>"
   Use the step name specified in the document, or create a short descriptive one if none is provided.

Do not skip any steps. Do not run audit commands or any project-wide analysis commands (such as npm run audit). Focus only on implementing the requested tasks and creating a backup after each completed step.

